'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; import {Search} from 'lucide-react'; import {api} from '../../../lib/api'; import {Loader,Empty,ErrorMessage,StatusBadge} from '../../../components/common/UI'; export default function Submissions(){const [rows,setRows]=useState(null),[q,setQ]=useState(''),[err,setErr]=useState('');useEffect(()=>{api.submissions().then(setRows).catch(e=>setErr(e.message))},[]);const filtered=rows?.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase()));return <><div className="page-title"><div><div className="eyebrow">REVIEW QUEUE</div><h2>Incoming records</h2><p>Open a record to review the source document and verify its structured fields.</p></div></div><div className="search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search document, citizen, village…"/><Search size={18}/></div><ErrorMessage>{err}</ErrorMessage><section className="card" style={{marginTop:16}}>{!rows?<Loader/>:!filtered.length?<Empty title="No incoming documents" text="No records match the current search."/>:<div className="table-wrap"><table className="data-table"><thead><tr><th>RECORD ID</th><th>CITIZEN</th><th>DOCUMENT</th><th>VILLAGE</th><th>SUBMITTED AT</th><th>STATUS</th><th></th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td>#{x.id}</td><td>{x.user?.name||'—'}</td><td>{x.document?.original_filename}</td><td>{x.document?.village}</td><td>{new Date(x.submitted_at).toLocaleString()}</td><td><StatusBadge status={x.status}/></td><td><Link className="button secondary" href={`/officer/review/${x.document_id}`}>Review record</Link></td></tr>)}</tbody></table></div>}</section></>}
