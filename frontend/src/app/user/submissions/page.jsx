'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; import {api} from '../../../lib/api'; import {getUser} from '../../../lib/auth'; import {Loader,Empty,ErrorMessage,StatusBadge} from '../../../components/common/UI'; export default function Submissions(){const [rows,setRows]=useState(null),[error,setError]=useState('');useEffect(()=>{api.userSubmissions(getUser().id).then(setRows).catch(e=>setError(e.message))},[]);return <><div className="page-title"><div><div className="eyebrow">MY RECORDS</div><h2>My Submissions</h2><p>Live status tracking for submitted land records.</p></div><Link className="button" href="/user/upload">Upload document</Link></div><ErrorMessage>{error}</ErrorMessage><section className="card">{!rows?<Loader/>:!rows.length?<Empty title="No submissions yet" text="Your uploaded land records will appear here."/>:<div className="table-wrap"><table className="data-table"><thead><tr><th>RECORD ID</th><th>DOCUMENT</th><th>TYPE</th><th>VILLAGE</th><th>SUBMISSION DATE</th><th>STATUS</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>#{x.id}</td><td>{x.document?.original_filename}</td><td>{x.document?.document_type}</td><td>{x.document?.village}</td><td>{new Date(x.submitted_at).toLocaleString()}</td><td><StatusBadge status={x.status}/></td></tr>)}</tbody></table></div>}</section></>}
