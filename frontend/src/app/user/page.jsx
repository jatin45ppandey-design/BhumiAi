'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {Files,Clock,CheckCircle,AlertTriangle,XCircle,RefreshCw} from 'lucide-react'; import {api} from '../../lib/api'; import {getUser} from '../../lib/auth'; import StatCard from '../../components/dashboard/StatCard'; import {Loader,Empty,ErrorMessage,StatusBadge} from '../../components/common/UI';
export default function UserDashboard(){const [stats,setStats]=useState(null),[rows,setRows]=useState([]),[error,setError]=useState('');async function load(){try{const u=getUser();const [s,r]=await Promise.all([api.userDashboard(u.id),api.userSubmissions(u.id)]);setStats(s);setRows(r.slice(0,5))}catch(e){setError(e.message)}}useEffect(()=>{load()},[]);if(!stats)return <Loader/>;return <><div className="page-title"><div><div className="eyebrow">MY RECORDS</div><h2>Land record overview</h2><p>Track your submitted records throughout review and verification.</p></div><Link className="button" href="/user/upload">Upload land record</Link></div><ErrorMessage>{error}</ErrorMessage><div className="stat-grid"><StatCard label="Total Submitted" value={stats.total_submitted} icon={Files}/><StatCard label="Pending" value={stats.pending_review} icon={Clock} tone="amber"/><StatCard label="Processing" value={stats.processing||0} icon={RefreshCw}/><StatCard label="Verified" value={stats.verified} icon={CheckCircle} tone="green"/><StatCard label="Needs Review" value={stats.needs_review} icon={AlertTriangle} tone="amber"/><StatCard label="Rejected" value={stats.rejected} icon={XCircle} tone="red"/></div><section className="card" style={{marginTop:20}}><div className="section-head"><div><h3>Recent submissions</h3><p>Live records from the platform database.</p></div><Link href="/user/submissions">View all</Link></div>{rows.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>SUBMISSION</th><th>DOCUMENT</th><th>STATUS</th><th>SUBMITTED</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>#{x.id}</td><td>{x.document?.original_filename||'Land record'}</td><td><StatusBadge status={x.status}/></td><td>{new Date(x.submitted_at).toLocaleDateString()}</td></tr>)}</tbody></table></div>:<Empty title="No submissions yet" text="Upload a scanned land record to begin AI-assisted verification."/>}</section></>}
