'use client'; import {useEffect,useState} from 'react'; import {useRouter} from 'next/navigation'; import PortalShell from '../../components/layout/PortalShell'; import {getUser} from '../../lib/auth'; import {Loader} from '../../components/common/UI'; export default function UserLayout({children}){const [ready,setReady]=useState(false),r=useRouter();useEffect(()=>{const u=getUser();if(!u||u.role!=='user')r.replace('/login');else setReady(true)},[r]);return ready?<PortalShell role="user">{children}</PortalShell>:<Loader label="Checking secure session…"/>}
