import './globals.css';
export const metadata={title:'Land Record Intelligence Platform',description:'Human-led land record verification'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
